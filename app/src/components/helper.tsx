export const line = (text: string) => (
  <div className="text-gray-300">{text}</div>
);

export const heading = (text: string) => (
  <div className="text-blue-400">{text}</div>
);

export const keyword = (text: string) => (
  <span className="text-yellow-400">{text}</span>
);

export const dim = (text: string) => (
  <span className="text-gray-500">{text}</span>
);

export const link = (text: string) => (
  <span className="text-purple-400">{text}</span>
);